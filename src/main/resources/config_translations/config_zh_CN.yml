# ═══════════════════════════════════════════════════════════════════════════════
# The Endex - 动态市场经济插件配置文件
# ═══════════════════════════════════════════════════════════════════════════════
# 修改配置后，请运行 /endex reload（需要管理员权限）或重启服务器。
#
# ═══════════════════════════════════════════════════════════════════════════════
# 🚀 已针对最小资源占用进行优化 (v1.5.8-JAN80235)
# ═══════════════════════════════════════════════════════════════════════════════
# 此配置默认预设为“低资源占用”。
# 非常适合中小型服务器，或希望将性能影响降至最低的服务器。
#
# 已应用的优化策略：
#   ✓ 世界存储扫描：已禁用（最主要的 CPU 节省项）
#   ✓ 每次更新保存：已禁用（减少磁盘 I/O）
#   ✓ 较长的更新间隔（降低计算频率）
#   ✓ 启用 SQLite 存储（比 YAML 更快）
#   ✓ 启用区块缓存（当使用世界扫描时）
#   ✓ 最小化历史记录长度（减少内存占用）
#
# 如需启用完整功能（会增加资源占用）：
#   - 设置 price-world-storage.enabled: true (开启动态稀缺性定价)
#   - 将 update-interval-seconds 减少到 60 (提高市场响应速度)
#   - 将 history-length 增加到 120 (更长的图表记录)
#
# ═══════════════════════════════════════════════════════════════════════════════
# 性能指南 (PERFORMANCE GUIDE)
# ═══════════════════════════════════════════════════════════════════════════════
# 每个选项都标记了其对性能的影响：
#   [PERF: MINIMAL]  - 影响极小，任何服务器都可安全使用。
#   [PERF: LOW]      - 低 CPU/内存占用，对所有服务器都很安全。
#   [PERF: MEDIUM]   - 中等影响，根据服务器规模进行调整。
#   [PERF: HIGH]     - 影响较大，针对大型服务器（50+ 玩家）需优化。
#   [PERF: CRITICAL] - 高资源占用，建议谨慎调整。
#
# 大型服务器建议：
#   - 增加 update-interval-seconds (建议 300-600)
#   - 禁用 price-world-storage 或限制 scanned-worlds
#   - 在 world-storage 中启用 chunk-caching
#   - 将 history-length 减少到 30-50
#   - 使用 SQLite 存储而非 YAML
# ═══════════════════════════════════════════════════════════════════════════════

# 内部版本号（用于检测更新）。请勿修改。
config-version: 1

# ───────────────────────────────────────────────────────────────────────────────
# 语言设置
# ───────────────────────────────────────────────────────────────────────────────
language:
  locale: zh_CN

# ───────────────────────────────────────────────────────────────────────────────
# 价格更新周期 [PERF: MEDIUM]
# ───────────────────────────────────────────────────────────────────────────────
# 市场根据供需关系重新计算价格的频率（秒）。
# 推荐：中小型服务器 60s，大型繁忙服务器 120-300s。
update-interval-seconds: 60

# ───────────────────────────────────────────────────────────────────────────────
# 价格灵敏度 [PERF: MINIMAL]
# ───────────────────────────────────────────────────────────────────────────────
# 控制供需失衡对价格影响的强度。
# 推荐值：0.05（每单位净需求引起 5% 的价格变化）
price-sensitivity: 0.05

# ───────────────────────────────────────────────────────────────────────────────
# 历史记录追踪 [PERF: LOW]
# ───────────────────────────────────────────────────────────────────────────────
# 为图表保留的过去价格点数量。
# 推荐：迷你图表（sparklines）用 5-10，详细网页图表用 50-120。
history-length: 5

# ───────────────────────────────────────────────────────────────────────────────
# 自动保存与备份 [PERF: LOW to MEDIUM]
# ───────────────────────────────────────────────────────────────────────────────
# 将市场数据保存到磁盘的频率（分钟）。
autosave-minutes: 5

# 每次价格更新后立即保存 market.yml（仅限 YAML 模式）。
# [PERF: 若开启则为 HIGH]
save-on-each-update: false

# ───────────────────────────────────────────────────────────────────────────────
# 数据存储模式 [PERF: MEDIUM to HIGH]
# ───────────────────────────────────────────────────────────────────────────────
# 强烈建议使用 SQLite (sqlite: true)：比 YAML 快 5-10 倍。
storage:
  sqlite: true

# ───────────────────────────────────────────────────────────────────────────────
# 价格平滑与波动保护 [PERF: MINIMAL]
# ───────────────────────────────────────────────────────────────────────────────
price-smoothing:
  enabled: true
  # EMA Alpha (0.0 - 1.0): 价格适应速度。推荐：0.3
  ema-alpha: 0.3
  # 每个周期允许的最大价格变化百分比。推荐：15.0
  max-change-percent: 15.0

# ───────────────────────────────────────────────────────────────────────────────
# 通货膨胀 / 通货紧缩系统 [PERF: MINIMAL]
# ───────────────────────────────────────────────────────────────────────────────
inflation:
  enabled: false
  # 每个周期的变化率。正数 = 通胀，负数 = 通缩。
  rate-per-cycle: 0.01
  category-rates:
    ores: 0.02
    food: 0.0
    building: 0.01
    mob-drops: 0.01
  # 仅在有玩家在线时应用。
  require-players: true
  # 偏离基础价格的最大百分比。
  max-deviation-percent: 200.0

# ───────────────────────────────────────────────────────────────────────────────
# 均值回归（防止操纵） [PERF: MINIMAL]
# ───────────────────────────────────────────────────────────────────────────────
# 当没有交易活动时，逐渐将价格拉回基础价格。
price-reversion:
  enabled: true
  # 每个周期缩小的价格差距比例。推荐：2.0%
  rate-percent-per-cycle: 2.0
  # 仅在当前周期没有交易时应用。
  only-when-idle: true

# ───────────────────────────────────────────────────────────────────────────────
# 基于玩家背包的价格影响 [PERF: LOW]
# ───────────────────────────────────────────────────────────────────────────────
# 根据在线玩家背包中的物品数量调整价格。
price-inventory:
  enabled: false
  sensitivity: 0.02
  # 每名玩家的基准量。超过此值价格下降，低于此值价格上涨。
  per-player-baseline: 64
  max-impact-percent: 10.0

# ───────────────────────────────────────────────────────────────────────────────
# 基于世界存储的价格影响 [PERF: HIGH to CRITICAL]
# ───────────────────────────────────────────────────────────────────────────────
# 扫描已加载区块中的箱子、木桶等容器。
# ⚠️ 这是资源消耗最高的项。
price-world-storage:
  enabled: false
  scan-interval-seconds: 300
  sensitivity: 0.01
  global-baseline: 1000
  max-impact-percent: 5.0
  # 每刻（tick）处理的区块数。推荐：50。
  chunks-per-tick: 50
  containers:
    chests: true
    barrels: true
    shulker-boxes: true
    hoppers: false
    droppers: false
    dispensers: false
    furnaces: false
    brewing-stands: false
  scan-shulker-contents: true
  excluded-worlds: []
  
  # 防止恶意操纵
  anti-manipulation:
    per-chunk-item-cap: 10000
    per-material-chunk-cap: 5000
    min-tps: 18.0
    log-suspicious: true

  # 智能缓存 [PERF: CRITICAL - 开启以获得最佳性能！]
  cache:
    enabled: true
    chunk-expiry-seconds: 600
    full-refresh-cycles: 5
    persist-to-disk: true

# ───────────────────────────────────────────────────────────────────────────────
# 市场初始设置
# ───────────────────────────────────────────────────────────────────────────────
# 仅在没有现有数据库时应用。
seed-all-materials: false
include-default-important-items: true

# ───────────────────────────────────────────────────────────────────────────────
# 交易税 [PERF: MINIMAL]
# ───────────────────────────────────────────────────────────────────────────────
# 所有买卖操作的税率（回收货币）。
transaction-tax-percent: 0.0

# ───────────────────────────────────────────────────────────────────────────────
# 买卖价差 (防止套利保护)
# ───────────────────────────────────────────────────────────────────────────────
# 在买入价和卖出价之间设置差价，防止刷钱漏洞。
spread:
  enabled: true
  # 玩家买入时额外支付的百分比。
  buy-markup-percent: 3.0
  # 玩家卖出时减少获得的百分比。
  sell-markdown-percent: 3.0

# ───────────────────────────────────────────────────────────────────────────────
# 反套利卖出冷却 [PERF: NEGLIGIBLE]
# ───────────────────────────────────────────────────────────────────────────────
anti-arbitrage:
  sell-cooldown:
    enabled: true
    duration-seconds: 60

# ───────────────────────────────────────────────────────────────────────────────
# 物品黑名单
# ───────────────────────────────────────────────────────────────────────────────
# 永远不会出现在市场中的材料（例如：[BEDROCK, BARRIER]）。
blacklist-items: []

# ───────────────────────────────────────────────────────────────────────────────
# 虚拟库存系统 (HOLDINGS) [PERF: LOW]
# ───────────────────────────────────────────────────────────────────────────────
# 虚拟存储空间。购买的物品会进入这里，之后可以物理取出。
holdings:
  enabled: true
  max-total-per-player: 100000
  auto-withdraw-on-login: false

# ───────────────────────────────────────────────────────────────────────────────
# 交付系统（已弃用 - 请使用 holdings）
# ───────────────────────────────────────────────────────────────────────────────
delivery:
  enabled: true
  auto-claim-on-login: false
  max-pending-per-player: 10000

# ───────────────────────────────────────────────────────────────────────────────
# 市场 GUI 自定义
# ───────────────────────────────────────────────────────────────────────────────
gui:
  # Shift+点击物品时显示迷你走势图。
  details-chart: true

# ───────────────────────────────────────────────────────────────────────────────
# 商店系统模式
# ───────────────────────────────────────────────────────────────────────────────
# DEFAULT = 滚动列表 / CUSTOM = 基于分类的商店。
shop:
  mode: DEFAULT
  main-shop: main
  command: shop
  custom:
    items-per-page: 45
    show-back-button: true
    back-button-slot: 49
    show-pagination: true
    prev-page-slot: 48
    next-page-slot: 50
    show-amount-button: true
    amount-button-slot: 45
    admin-edit: true
    # GUI 中的价格信息格式。
    price-lore:
      - "&7"
      - "&a左键: &f购买: &6$%buy_price%"
      - "&c右键: &f卖出: &6$%sell_price%"
      - "&7"
      - "&8库存: %stock% | 需求: %demand%"
    admin-lore: "&e⚡ Shift + 点击: 编辑物品 (管理员)"

# ───────────────────────────────────────────────────────────────────────────────
# 投资系统 [PERF: LOW]
# ───────────────────────────────────────────────────────────────────────────────
# 玩家可以投资物品以赚取复利。
investments:
  enabled: true
  # 年化收益率 (APR)。推荐：5.0
  apr-percent: 5.0

# ───────────────────────────────────────────────────────────────────────────────
# 历史数据导出 (分析)
# ───────────────────────────────────────────────────────────────────────────────
history-export:
  enabled: true
  folder: history

# ───────────────────────────────────────────────────────────────────────────────
# 资源追踪
# ───────────────────────────────────────────────────────────────────────────────
tracking:
  resources:
    enabled: true
    # 是否将采集到的资源自动添加到市场“供应”中？
    # ⚠️ 警告：可能导致大规模通货紧缩。推荐：false。
    apply-to-market: false
    sources:
      block-break: true
      mob-drops: true
      fishing: true

# ───────────────────────────────────────────────────────────────────────────────
# 市场活动
# ───────────────────────────────────────────────────────────────────────────────
events:
  # 叠加倍率的上限。
  multiplier-cap: 10.0

# ═══════════════════════════════════════════════════════════════════════════════
# 网页控制面板配置 [PERF: LOW to MEDIUM]
# ═══════════════════════════════════════════════════════════════════════════════
web:
  enabled: true
  # 生产环境下建议配合反向代理 (nginx) 使用 127.0.0.1。
  host: 127.0.0.1
  port: 3434
  session-duration-hours: 2
  
  # 响应压缩 [PERF: LOW - 节省带宽]
  compression:
    enabled: true
    level: 4

  # 实时更新方案
  poll-ms: 1000
  history-limit: 120
  sse:
    enabled: false
  websocket:
    enabled: true
  
  # API 频率限制 (安全)
  rate-limit:
    enabled: true
    requests: 30
    per-seconds: 10
    exempt-ui: true
  
  # 权限与角色
  roles:
    default: TRADER
    trader-permission: endex.web.trade
    admin-view-permission: endex.web.admin
  
  # 网页端显示游戏图标
  icons:
    enabled: false
    source: ""
  
  # 插件集成
  addons: []
  
  # 自定义网页 UI 覆盖
  custom:
    enabled: false
    root: webui
    reload: false
    export-default: true
  
  # 库存财产 API
  holdings:
    inventory:
      enabled: true
      include-enderchest: false
      cache-seconds: 15

# ═══════════════════════════════════════════════════════════════════════════════
# 日志与调试 [PERF: LOW - 详细模式会增加磁盘 I/O]
# ════───────────────────────────────────────────────────────────────
logging:
  # 仅在排查问题时启用。会显著增加日志文件大小。
  verbose: false

# ═══════════════════════════════════════════════════════════════════════════════
# 更新检查器
# ═══════════════════════════════════════════════════════════════════════════════
update-checker:
  enabled: true
  notify-ops: true

# ═══════════════════════════════════════════════════════════════════════════════
# 配置文件结束
# ═══════════════════════════════════════════════════════════════════════════════